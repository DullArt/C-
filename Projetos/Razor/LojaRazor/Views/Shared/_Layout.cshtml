<DOCTYPE html>
<html>
    <head>
        @Style.Render("~/bundles/estilos")
    </head>
    <body>
        @Html.Partial("_MenuSuperior")
            <div class="container-fluid">
                <div class="row-fluid">
                    <div class="span3">
                    @Html.Action("Index", "Menu")
                    </div>
                <div class="span9">
                    //as partes especificas de cada corpo 
                    @RenderBody();
                </div>
            </div>
        </div>

        @RenderSection("Scripts", required: false)
    </body>
</html>
